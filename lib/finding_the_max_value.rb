require 'pry'

def find_max_value(array)
  index = 0
  max_value = -1
  while count < array.length do
    if max_value < array[index]
      max_value = array[index]
    end
    count += 1
  end
  max_value
  end
